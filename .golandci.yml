version: "2"
run:
  timeout: 3m
  tests: true

linters:
  enable:
    - govet
    - revive
    - errcheck
    - staticcheck
    - unused
    - gosimple

issues:
  exclude-use-default: false

formatters:
  enable:
    - gofumpt
    - goimports
    - golines